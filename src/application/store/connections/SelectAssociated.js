export default {
    props: (store) => {

        var associations = Array.isArray(store.associations) ? store.associations : [];

        return {

        }
    },
    dispatches: () => {
        return {};
    },
}